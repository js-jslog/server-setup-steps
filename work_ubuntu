1. Modify host key from ctrlRight (is it this which breaks the alt-tab)
2. `ssh-keygen -t rsa -b 4096`
3. sudo apt-get install connect-proxy
4. set neovim to be default editor by adding the following to .bashrc
```
export EDITOR='nvim'
```
